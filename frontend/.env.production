# Production build: API is on the same origin (altistravels.com),
# so VITE_API_URL is intentionally empty — no trailing slash.
# The frontend code will fall back to window.location.protocol + hostname (port-free).
VITE_API_URL=
